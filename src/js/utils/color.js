// getContrast(color1, color2)

const WCAG = 'wcag2';
const APCA = 'apca';

/**
 * Color object in RGB format
 * @typedef {Object} RGB
 * @typedef {Integer} object.r
 * @typedef {Integer} object.g
 * @typedef {Integer} object.b
 */


/**
 * To-do:
 * - Support alpha values
 * @param  {RGB} textColor
 * @param  {RGB} backgroundColor
 * @param  {String} contrast mode
 * @return {String}
 */
export function getContrast (textColor, backgroundColor, mode = WCAG) {

}

function wcagContrast() {

}

